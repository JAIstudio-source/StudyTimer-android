package com.madeby.JAI

enum class AppPanel { FOCUS, STATS, SETTINGS, HEATMAP }
enum class TimerState { IDLE, STUDYING, BREAK, PAUSED }
enum class AppStatsTab { OVERVIEW, TIMELINE }
enum class AppSettingsTab { SIMPLE, THEME }
